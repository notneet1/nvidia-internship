[project]
name = "nvidia-internship"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyterlab>=4.6.2",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
]

[project.scripts]
nvidia-internship = "nvidia_internship:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
